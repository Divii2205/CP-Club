#include <bits/stdc++.h>
using namespace std;

void printPaths(int cur, int n, string path)
{
    // doing our work then calling recursion
    printPaths(arr, curIdx, )
}
int main()
{
    int n; cin >> n;
    getWays(0, n, "");
}