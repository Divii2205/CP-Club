#include<bits/stdc++.h>
using namespace std;

vector<vetor<int>> getSubsets(vector<int> &a)

int main()
{
    vector<int> a = {4,6,8};
    vector<vetor<int>> subsets = getSubsets(a);
    for(auto &subset : subset)
    {
        
    }
}